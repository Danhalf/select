const defaultDependency = require('./default');
const html = require('./html');
const css = require('./css');
const minify = require('./minify');
const images = require('./images');

module.exports = { defaultDependency, html, css, minify, images };
